package org.example;

import java.util.List;
import java.util.Optional;
import java.util.stream.Stream;

public class OptionalTest {
//    List<Address> addressList = Optional.of();
//    Optional<List<Address>> addressList = Optional.of(Stream.of(new Address("bedok","bugis")).toList());
//    addressList.flatMap(Address::getCountry).orElse(new Address("fuckst","fuckst"));
}
